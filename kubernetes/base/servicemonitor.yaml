apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: discord-bot
  namespace: discord-bot
  labels:
    app: discord-bot
spec:
  selector:
    matchLabels:
      app: discord-bot
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
